apiVersion: v1
kind: Service
metadata:
  name: policy-docs
  namespace: policy-docs
  labels:
    app.kubernetes.io/name: policy-docs
    app.kubernetes.io/part-of: ggi-policy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: policy-docs
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
