apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: policy-docs
  labels:
    app.kubernetes.io/name: oauth2-proxy
    app.kubernetes.io/part-of: ggi-policy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oauth2-proxy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
